{
  "name": "pi-desktop",
  "private": true,
  "version": "0.3.4",
  "license": "MIT",
  "packageManager": "pnpm@10.25.0",
  "scripts": {
    "dev": "pnpm --filter @pi-desktop/desktop dev",
    "build": "pnpm -r --if-present build",
    "typecheck": "pnpm --filter @pi-desktop/session-driver build && pnpm --filter @pi-desktop/catalogs build && pnpm --filter @pi-desktop/pi-sdk-driver build && pnpm -r --if-present typecheck",
    "lint": "pnpm -r --if-present lint",
    "test": "pnpm -r --if-present test",
    "e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0",
    "@types/node": "^24.0.0",
    "typescript": "^5.9.3"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild",
      "electron",
      "electron-winstaller",
      "koffi",
      "sharp",
      "protobufjs"
    ],
    "patchedDependencies": {
      "@mariozechner/pi-ai@0.64.0": "patches/@mariozechner__pi-ai@0.64.0.patch"
    }
  }
}
